const button = document.getElementById("button");
console.log(button);

window.addEventListener("DOMContentLoaded", function(){
    // document 객체 변환 완료
    const button = document.getElementById("button");
    console.log(button);
})